# docker-compose run app
version: '3'
services:
  app:
    image: jun552313/python:cabocha
    tty: true
    volumes:
      - ./:/work:cached
    working_dir: /work
    environment:
      - HOME=/work
    command: /bin/bash
